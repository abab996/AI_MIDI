# 更新日志 / Changelog

## v2.0.0（2026-08-06）— 从"快捷操作工作台"到 AI 编曲桌面应用

> 首个版本（v1）是一个命令行交互工具：固定解析 `input/in.mid`，通过菜单选择
> **配和弦 / 翻译歌词 / 设计转音 / 其他要求** 四种快捷操作，手动输入 BPM、拍号、
> 歌词与语言等参数，把结果写成 MIDI（note_table）或纯文本。
>
> v2.0 是一次彻底的跨越——**70 个提交、约 1.8 万行新增代码**——将 v1 重铸为
> 本地桌面级 Web 应用：原生窗口 + 图形界面 + 多轮对话 + 项目管理，同时保留了
> v1 全部四种快捷操作并为其配上了可视化工作台。

### ✨ 新功能

- **图形界面（替代命令行交互）**：FastAPI 本地服务 + 三个页面——主工作台 `/`、
  多轮对话 `/chat.html`、设置 `/settings.html`；默认 pywebview 原生窗口，`--browser`
  可改用系统浏览器打开
- **多轮对话模式**：项目档案库 + 对话工作台双视图，由 **MCP 工具调用**驱动——
  AI 可在你的项目中创建、解析、整理、删除 MIDI 文件，每一步以可折叠工具块展示
- **乐理知识库**：内置 20 份系统化知识库（乐理基础、配和弦、对位、配器、曲式、
  转调、即兴模板等），AI 在对话中**按需主动调用**，而非全量注入
- **工作区绑定**：关联本地文件夹，文件双向同步
- **撤销 / 恢复**：每个文件操作可逆（带回收站）
- **跨项目搜索**：在全部对话与项目中全文检索
- **思考过程可视化**：推理内容以流式思考块实时展示，思考完毕立即收起
- **设置页持久化**：API Key、Base URL、模型、生成参数写入 `settings.json`
  （已被 git 忽略，密钥永不入库）
- **任意 OpenAI 兼容服务商**：DeepSeek / OpenAI / Gemini（自动拼接 `api_path`）/
  本地 LLM，设置页可一键刷新模型列表
- **流式输出 + 上下文自动压缩**：长对话按 400k 字符上限自动摘要压缩，保持上下文不炸
- **双主题**：暖纸 ⇄ 暗蓝，`localStorage` 记忆；Q 弹交互动画（卡片变形、涟漪、惯性回弹）

### 🚀 体验升级

- 拖入或点击选择 MIDI → 「解析 MIDI」→ 选择任务 → 实时流式控制台输出 → 一键下载结果
- 音名规范化、AI 输出多格式兼容、note_table 字段顺序无关解析
- 知识库注入方式重构：全量注入 → AI 按需调用（减少 token 消耗、提升工具使用率）
- 草稿按项目自动存档，切换项目自动恢复
- 启动画面（splash）像素级透明 + 平滑淡入淡出

### 🔧 主要修复

- 流式中途崩溃、工具调用 400 错误（Gemini `thought_signature` 适配）
- 设置保存崩溃；`max_tokens` 默认 0 导致的请求异常
- 服务器暴露风险（仅绑定 `127.0.0.1`）；`base_url` 强制 https + 443 且禁路径
- MCP 工具路径穿越防护；子进程 stderr 重定向防死锁；接收超时兜底
- MIDI 解析容错：重复 `note_on`、缺失 `note_off` 等真实文件兼容
- 打包后 GBK 编码崩溃；打包版双击启动失败（windowed 无控制台下
  `sys.stdout/stderr` 为 None）
- 思考块动画、布局错乱、打开项目后返回按钮失效等一批 UI 问题

### 📦 打包与发布

- **PyInstaller 多文件打包**：`AI_MIDI.exe` + `_internal`（完整依赖），splash 多尺寸图标
- **Inno Setup 安装包**：中英双语向导、许可页、桌面/开始菜单快捷方式、
  卸载时保留用户数据（可选删除）
- Apache-2.0 许可；README 中英双语拆分互链

### 🧪 质量与安全

- pytest 测试套件：11 个模块、2300+ 行测试（API 层、管道、服务、MCP、
  配置、路径安全）
- 日志系统补全，日志中不记录 API Key

### ⬇️ 升级方式

- 源码运行：`git pull` 后 `pip install -r requirements.txt`，`python main.py`
- 打包版：使用新的安装包（卸载旧版时选择**保留**用户数据即可无损升级）
