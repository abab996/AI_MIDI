# AI_MIDI 音频路线严格化改造方案

> 日期：2026-09-06
> 目标：**一切播放音频的地方都严格遵循设置页的音频路线**——设置为"音频引擎"时只允许走原生引擎（JUCE），任何场景都不得使用 WebAudio；设置为"浏览器合成"时才使用 WebAudio。同时并入此前排查发现的全部遗留问题。

---

## 一、现状盘点

### 1.1 已修复（提交 `acbe1ef` / `e0ea78f`，已核对并独立复测）

| # | 问题 | 修复 |
|---|------|------|
| D1 | 钢琴窗 SF2 预设完整 id 被 `replace("sf2_","")` 剥坏，`setPreset` 找不到 → 上传音源"选了没反应/无声" | 完整 id 直传 + `setPreset` 前缀兼容 |
| D2 | 切合成波音色后第一下 noteOn 发往仍持旧声部的引擎轨（旧音色瞬态） | `_ensureNativeVoice` 未确认前不判定原生可用 |
| D3 | 播放路径从不调用 `setWaveform`，`synth.waveform` 停留旧值 → 播放时切锯齿/方波/三角/正弦听不出变化 | `_scheduleTick` 原生分支 + 下拉 onChange 同步波形 |
| D4 | SF2 zone 解析把 inst 记录按 38 字节步进（规范 22 字节）→ zone 链断裂、音符全被移调到最低音采样 | `readNdx("inst", 22, 20)`；实测 Pianoteq 216 zone、GeneralUser 269 预设全部正确 |

### 1.2 工作区已有但未提交（本次方案已纳入，见 §5.6 / §5.7）

| # | 改动 | 位置 |
|---|------|------|
| W1 | 内置音色生成失败可重试（避免 `builtinBuffers` 卡空对象永久静默） | `frontend/js/pianoroll/soundfont.js` |
| W2 | SF2 上传上限 32MB → 256MB（handler 与 router 双层） | `internal/server/handler_soundfont.go`、`internal/server/router.go` |

### 1.3 现存问题（本方案要解决的）

按"路线严格化"（P0 类）与"功能/健壮性"（P1/P2 类）分组：

**P0 —— 违反"引擎模式只用引擎"的 WebAudio 路径（必须消灭）：**

| # | 位置 | 现状 |
|---|------|------|
| P0-1 | 钢琴窗 `playNoteSound` / `_scheduleTick` 的 `sf2_*` 分支 | 纯 WebAudio（SoundFontPlayer），引擎模式也走 WebAudio |
| P0-2 | 钢琴窗内置"温暖钢琴/弦乐群"（`sf2_piano`/`sf2_strings`） | 纯 WebAudio（代码合成采样缓冲） |
| P0-3 | 钢琴窗节拍器 `synth.playClick`（播放 L1022、预备拍 L776） | 纯 WebAudio（createOscillator） |
| P0-4 | 编曲窗 `_triggerDue` 的 builtin 轨 | 纯 WebAudio（`nodes.soundfont.setPreset(src.tone)`） |
| P0-5 | 编曲窗 `loadTrackSoundFont` | 原生优先，失败/无盘路径**回退 WebAudio** |
| P0-6 | 编曲窗 `_ensureTrackWaveVoice` | `setTrackVoice` 失败/未确认期间该轨**回退 WebAudio**（`_useNative=false`、`_forceWebAudio=true`） |
| P0-7 | 编曲窗节拍器（audio_engine.js L698/883/977） | 纯 WebAudio（createOscillator） |
| P0-8 | 编曲窗音源试听（arrange.js L3872 `previewTrackSource`） | `synth._forceWebAudio = true` 强制 WebAudio |
| P0-9 | `synth.js noteOn/noteOff` | `_ensureNativeVoice` 未确认时落 WebAudio；`_forceWebAudio` 分支 |
| P0-10 | `SynthEngine` / `SoundFontPlayer` / `ArrangeEngine` | 引擎模式下仍会创建 AudioContext（SharedAudio）——存在即风险 |

**P1 —— 引擎能力缺口（让上述路径"原生化"所必需的引擎侧能力）：**

| # | 缺口 | 说明 |
|---|------|------|
| P1-1 | 引擎无预设选择 IPC | tsf 有 `tsf_channel_set_bank_preset`，但未暴露；多预设 SF2 与内置音色映射都用得上 |
| P1-2 | 内置 piano/strings 无原生载体 | 需映射到随包分发的 GeneralUser GS (0,0)/(0,48)，或引擎内合成 |
| P1-3 | 引擎无节拍器 click | 需新增 click 声部/IPC |
| P1-4 | 前端拿不到内置音色库绝对路径 | `/api/audio/status` 需增 `defaultSoundfont` |

**P2 —— 其他遗留问题（一并处理）：**

| # | 问题 | 位置 |
|---|------|------|
| P2-1 | 加载无预设的 SF2 时 toast 仍提示"已加载音色库"，实际音源未切换（误导） | `pianoroll.js refreshSoundLibraryList` / `updateSoundSelectOptions` |
| P2-2 | 编曲窗轨数 ≥31 时与钢琴窗专用轨 31（PERF_TRACK）冲突（引擎 32 轨上限） | `engine_bridge.js` / `audio_engine.js` |
| P2-3 | webaudio 模式下 AudioContext 恢复依赖触发时机（MIDI 触发 noteOn 非用户手势，resume 可能被拒） | `pianoroll/audio_context.js` / 各 `resume()` |
| P2-4 | 引擎崩溃/重启期间引擎模式无任何出声与明确提示（只有静默） | `chat.html` 徽章 / 播放门控 |

---

## 二、核心设计：统一音频路由

### 2.1 路线语义（收紧）

现行设置：`settings.audio.backend ∈ {"auto","webaudio"}`、`settings.audio.engine_enabled`。

- **`auto`（默认）→ 语义改为"ENGINE · 仅音频引擎（不降级）"**：
  - 引擎启用且就绪：**所有发声只走原生引擎**；任何 WebAudio 路径（含"回退""试听强制""未确认期间""失败降级"）一律删除/禁用。
  - 引擎未就绪/崩溃/声部设置失败：**不发声 + 明确 UI 状态**（播放门控、徽章提示、错误 toast），绝不回退 WebAudio。
- **`webaudio` → "WEBAUDIO · 浏览器合成"**：全部走 WebAudio，维持现有功能（并顺带做 P2-3 的 resume 加固）。
- 设置页文案同步更新（`settings.html` 的 AUTO 按钮改为"ENGINE · 仅引擎"）。

### 2.2 路由单点化

- 扩展 `AudioBackend`（`synth.js` 内）为全局唯一路由判定：
  - `mode()` → `"engine" | "webaudio"`（由 `backend` + `engine_enabled` + 引擎 state 推导，Go 为权威源）。
  - `isEngine()` / `isWebAudio()`。
- **删除散落的模式判定与回退**：`_useNative`、`_forceWebAudio`、`nodes._useNative`、`isNativePreferred()` 中的降级语义统一收敛到 `AudioBackend`，发声组件只按 `mode()` 分派。
- **引擎模式下不创建 AudioContext**：`SynthEngine`/`SoundFontPlayer`/`ArrangeEngine` 的 `init()` 在 `mode()==="engine"` 时跳过 WebAudio 节点构建（杜绝"存在即可能被用"）。

---

## 三、分系统修改方案

### 3.1 引擎（C++）——补齐 P1

**P1-1 预设选择 IPC `setTrackPreset`**

- `SynthEngine.h/.cpp`：新增
  ```cpp
  // 持 loadMutex_；无 tsf 返回 false；tsf_channel_set_bank_preset 返回 0（预设不存在）→ false
  bool setTrackPreset(int bank, int program);
  ```
- `EngineAudioDevice.h/.cpp`：`juce::String setTrackPreset(const juce::var& params)`（解析 `{track, bank, program}`，track 越界钳制，与 `setTrackVoice` 同款纪律）。
- `Main.cpp`：注册 IPC 方法 `setTrackPreset`（消息线程 `callAsync`，与 `loadSoundFont`/`setTrackVoice` 并列）。
- 重建：`tools/build_engine.bat` → `bin/aimidi-engine.exe`。

**P1-3 节拍器 click**

- 方案 A（推荐，改动小）：新增 IPC `click {track, high}`——复用该轨 WaveSynth，用极短包络（attack≈0.001s、release≈0.04s）的方波/正弦做木鱼音，`high` 区分重拍/弱拍频率（同前端语义 1600/900Hz）。
- 方案 B（彻底）：Transport 内建 metronome 按拍自动触发。改动大，留作后续。
- 前端仍按现有调度器按拍调用（仅把输出端从 `synth.playClick` 换成 `EngineBridge.click`）。

**P1-2 内置音色原生载体**

- 引擎模式：内置 `piano`/`strings` 映射到随包 GeneralUser GS 的 `(0,0)` / `(0,48)`（已确认存在："Stereo Grand"/"Stereo Strings Fast"）。
- 依赖 P1-1 的 `setTrackPreset` + 前端拿到 `defaultSoundfont` 路径（P1-4）。

### 3.2 Go 后端（internal/engine + internal/server）

| 项 | 改动 |
|----|------|
| P1-1 后端 | `supervisor.go`：`SetTrackPreset(track, bank, program int) error` → `cli.Request("setTrackPreset", ...)`；新增 `lastPresets map[int][2]int`，会话重放顺序：`loadSoundFont` → **`setTrackPreset`** → `setTrackVoice`（参照现有 L787-818 段）。 |
| P1-1 绑定 | `app.go`：`EngineSetTrackPreset(track, bank, program int) error`（Wails 自动绑定；bridge 走 `window.go.app.App.*` 原始命名空间，无需重新生成 wailsjs）。 |
| P1-4 | `types.go` 状态结构 + `handler_audio.go`：`/api/audio/status` 增 `defaultSoundfont`（`defaultSoundFontPath()` 绝对路径，空表示未安装内置音色库）。 |
| 门控 | 前端引擎模式门控以现有 `status.state == "ready"` 为准；`engine_enabled=false` 或 `backend="webaudio"` → 全部 WebAudio（现状保留）。 |
| W2 | 上传上限 256MB 已在工作区（handler + router），随本方案一并提交。 |

### 3.3 前端公共（engine_bridge.js / synth.js / audio_context.js）

- `engine_bridge.js`：新增 `setTrackPreset(track, bank, program)`、`click(track, high)`；`getBackend()`/`isNativePreferred()` 语义收紧（不再代表"可降级"）。
- `synth.js`：
  - `AudioBackend` 扩展为唯一路由（§2.2），设置页/启动时与 Go 同步（现有 `fetch /api/audio/settings` 逻辑保留）。
  - `_ensureNativeVoice`：引擎模式下未确认期间**不再回退 WebAudio**——音符暂存（pending 队列，确认后按新声部重发；播放路径 150ms 提前量天然覆盖；实时键盘暂存约 10ms）。备选简化方案：直接直发并接受首音瞬态（与编曲窗一致），在切换音色时立即预热 `setTrackVoice`。
  - `noteOn/noteOff`：删除 `_forceWebAudio` 分支；引擎模式无 WebAudio 兜底。
  - `playClick`：引擎模式改走 `EngineBridge.click`；仅 webaudio 模式保留 Oscillator 实现。
  - `setVolume/setFilter`：引擎模式同步到引擎轨参数（重发 `setTrackVoice`，签名已含）。
- `audio_context.js`：仅 webaudio 模式创建/使用；P2-3 加固（全局一次性 `pointerdown`/`keydown` → `SharedAudio.resume()`，覆盖 MIDI 触发场景）。

### 3.4 钢琴窗（pianoroll.js）

- **C2 SF2 原生轨**：新增专用轨常量 `SF2_TRACK = 30`（31 继续用于波形声部）。
  - `playNoteSound` / `_scheduleTick` 的 `sf2_*` 分支（引擎模式）：
    1. 首次/切换时异步 `EngineBridge.loadSoundFontTrack(30, path)` + `EngineBridge.setTrackPreset(30, preset.bank, preset.program)`（带 pending/3s 冷却；失败 → 该音源标记不可用 + 错误提示，**不回退 WebAudio**）。
    2. noteOn/noteOff → `noteOnTrack(30)` / `noteOffTrack(30)`（`_schedNativeNote` 已支持任意 trk，直接复用）。
    3. 切音源时清掉轨 30 残留音符；`stopAllSounds` 补充轨 30 停音。
  - SF2 路径来源：IndexedDB 记录 `diskPath`（上传镜像绝对路径）优先，否则 `Library/soundfonts/<safe>.sf2`（引擎 CWD 相对，与编曲窗同规则）。
- **C3 内置音色**：引擎模式 `sf2_piano`/`sf2_strings` → `loadSoundFontTrack(30, status.defaultSoundfont)` + `setTrackPreset(30, 0, 0|48)`；`defaultSoundfont` 为空（未安装内置音色库）→ 明确提示"缺少内置音色库"，不回退。
- **C5 清理**：引擎模式下不再实例化/初始化 WebAudio 发声器（`SoundFontPlayer` 仅在 webaudio 模式使用；其 parseSF2 解析能力在引擎模式仍可用于读取预设元数据/预设列表——区分"解析"与"发声"两个职责）。
- 节拍器：见 3.1 P1-3 + 3.3（播放 L1022、预备拍 L776 输出端切换）。

### 3.5 编曲窗（audio_engine.js / arrange.js）

- **D1 `_triggerDue`**：引擎模式 strict——synth/sf2/builtin 全部只走引擎；删除 WebAudio 分支。
- **D2 `loadTrackSoundFont`**：引擎模式加载失败 → `onSoundFontError` 上浮 UI 提示（轨保持静音并显示错误态），删除 `.catch` 中的 WebAudio 回退解析。
- **D3 builtin 轨**：引擎模式映射 GeneralUser（同 C3）；webaudio 模式保留 `SoundFontPlayer.setPreset(src.tone)`。
- **D4 `_ensureTrackWaveVoice`**：失败/未确认期间不再 `_useNative=false`/`_forceWebAudio=true`——该轨标记"未就绪"静音 + 3s 重试，恢复后自动出声。
- **D5 试听 `previewTrackSource`**（arrange.js L3872）：删除 `_forceWebAudio = true`；引擎模式试听发到专用试听轨（如 29：按源的 wave 发 `setTrackVoice(29)`，SF2 发 `loadSoundFontTrack(29)`+`setTrackPreset`+`noteOnTrack`），webaudio 模式保留现行为。
- **D6 节拍器**：同 3.3（audio_engine.js L698/883/977 输出端切换）。
- **D7 轨位冲突**：编曲轨索引上限约束为 0..28（预留 29 试听、30 钢琴窗 SF2、31 钢琴窗波形），超出报错提示"编曲轨数达到引擎上限"（或在引擎侧扩轨——不在本方案）。

### 3.6 设置页（settings.html / settings.js）

- 后端选择文案：`AUTO · 原生引擎优先` → `ENGINE · 仅音频引擎（不降级）`；`WEBAUDIO · 浏览器合成` 不变。
- 引擎模式提示：引擎未就绪/已禁用时，播放类入口显示"音频引擎未就绪，播放不可用"。

---

## 四、其他问题处理（P2）

| 项 | 处理 |
|----|------|
| P2-1 空预设误导 | `refreshSoundLibraryList` 加载后：`parsed.presets.length === 0` → toast 改为"✗ 该文件没有可用预设，音源未切换"；`updateSoundSelectOptions` 保持当前音源（现有早退逻辑）。 |
| P2-2 轨位冲突 | 见 D7（编曲 ≤28 约束 + 专用轨位表文档化）。 |
| P2-3 webaudio resume | 全局首次交互 resume（3.3），并给 `resume()` 的 rejection 加 `console.warn`（诊断）。 |
| P2-4 引擎不可用提示 | `chat.html` 引擎徽章在引擎模式下展示"引擎未就绪 · 播放不可用"；播放按钮点击时若 `mode()==="engine"` 且引擎未 ready → toast 阻断并跳转设置页。 |

---

## 五、边界与失败模式

1. **引擎未就绪（启动中/崩溃/禁用）**：引擎模式播放被阻断并提示；不静默、不回退 WebAudio。supervisor 崩溃重启 + 声部/音色重放机制保留（`lastVoices`/`lastSoundFonts`/`lastPresets`）。
2. **`setTrackVoice`/`setTrackPreset`/`loadSoundFont` 失败**：该轨/音源静音 + 3s 冷却重试 + 错误提示；恢复后自动出声，不降级。
3. **缺少内置音色库文件（dev 环境）**：`defaultSoundfont` 为空 → 内置 piano/strings 在引擎模式明确报错；webaudio 模式用生成缓冲（现状）。
4. **>256MB SF2**：拒绝上传（合理上限，提示文案同步）。
5. **webaudio 模式**：完整保留现有 WebAudio 功能与 P2-3 加固，不受引擎影响。
6. **引擎模式轨位分配表**：0-28 编曲、29 试听、30 钢琴窗 SF2、31 钢琴窗波形/实时键盘（写入 `engine_bridge.js` 注释与本文档，防误用）。
7. **预设不存在**（`setTrackPreset` 返回 false）：回退该文件 preset 0 并提示"预设不存在，已用首个预设"。

---

## 六、验证与验收

### 6.1 自动化

- **Node 回归**（`tools/` 新增脚本，固化 D4 验证）：
  - 用真实 `soundfont.js` 解析 `Library/soundfonts/*.sf2`，断言 Pianoteq 216 zone、GeneralUser 269 预设均有 zone、note 60 命中 root=60。
  - headless Chrome：固定解析器下 `noteOn(60)` 输出可闻（>阈值 RMS）。
- **Go 测试**：`SetTrackPreset` 参数构造、`lastPresets` 重放顺序（`loadSoundFont → setTrackPreset → setTrackVoice`）、上传 256MB 上限回归（构造 >32MB 的假 SF2 断言可过、>256MB 断言拒绝）。
- **引擎冒烟**：`tools/engine_smoke` 增加 `setTrackPreset`、`click` 两项。

### 6.2 手动验收（关键场景矩阵）

| 场景 | engine 模式预期 | webaudio 模式预期 |
|------|----------------|-------------------|
| 钢琴窗 播放/实时键盘 切 锯齿/方波/三角/正弦 | 音色即时变化，全程引擎 | 同，WebAudio |
| 钢琴窗 内置 温暖钢琴/弦乐群 | 有声音（GeneralUser 0/0、0/48） | 有声音（生成缓冲） |
| 钢琴窗 加载 Pianoteq 后选预设 | 有声音、音高正确（216 zone） | 同 |
| 钢琴窗 节拍器 / 预备拍 | 引擎 click | WebAudio click |
| 编曲窗 synth/builtin/SF2 轨播放 | 全引擎，无 WebAudio | 全 WebAudio |
| 编曲窗 音源试听 | 引擎试听轨 | WebAudio |
| WAV 导出（bounce） | 引擎离线渲染（现状） | 不适用（bounce 需引擎） |
| 引擎崩溃/禁用 | 播放被阻断 + 明确提示，**绝不 WebAudio** | 不受影响 |
| 无引擎环境（Linux 浏览器模式） | — | 全功能（现状） |

### 6.3 回归

- 编曲窗 SF2 原生加载、多轨混音、BPM 变速、循环、WAV 导出尾音不截断。
- 引擎冷启动重放：`lastSoundFonts` → `lastPresets` → `lastVoices`。
- 设置页：后端切换（engine↔webaudio）即时生效且状态一致。

---

## 七、实施阶段

### P0 —— 严格路由核心（前端为主，可独立上线）
- §2.1/2.2 路由收紧 + 设置页文案；§3.3 `AudioBackend` 单点化。
- §3.4 C1/C5、§3.5 D1/D4/D5、§3.6：删除全部"引擎模式回退 WebAudio"路径（P0-1 除外，见下），引擎模式不创建 AudioContext。
- P0-1/P0-2（钢琴窗 SF2/内置原生化）依赖 P1，故 P0 期间钢琴窗 SF2/内置在引擎模式下表现为"明确提示不可用"（临时），避免静默无声或误用 WebAudio。

### P1 —— 引擎能力补齐（C++ + Go + 前端，一次联调）
- §3.1 P1-1（`setTrackPreset`）+ P1-3（`click`）+ P1-2（内置映射）；§3.2 全部；`tools/build_engine.bat` 重建引擎。
- §3.4 C2/C3、§3.5 D2/D3/D6：钢琴窗与编曲窗 SF2/内置/节拍器全部原生化。
- P2-1 一并处理（纯前端）。

### P2 —— 收尾
- P2-2 轨位约束、P2-3 webaudio resume 加固、P2-4 引擎不可用提示；文档同步（README/协议文档/本文档轨位表）。

---

## 八、涉及文件清单

**前端**
- `frontend/js/pianoroll/pianoroll.js`、`synth.js`、`soundfont.js`、`audio_context.js`
- `frontend/js/arrangement/audio_engine.js`、`arrange.js`
- `frontend/js/engine/engine_bridge.js`
- `frontend/settings.html`、`frontend/js/settings.js`、`frontend/chat.html`

**Go**
- `internal/engine/supervisor.go`、`app.go`（在 `internal/app/`）、`types.go`、`internal/server/handler_audio.go`、`handler_soundfont.go`、`router.go`（后两者 W2 已在工作区）

**C++**
- `engine/Source/Audio/SynthEngine.h/.cpp`、`EngineAudioDevice.h/.cpp`、`Source/Main.cpp`，重建 `bin/aimidi-engine.exe`

**测试/工具**
- `tools/` 新增 SF2 解析回归脚本；`internal/engine/*_test.go` 新增用例；`tools/engine_smoke` 增项

---

## 九、备注（与既有行为的一致性）

- 现有"引擎优先、WebAudio 兜底"是 v3.0.x 的默认行为；本方案将其收紧为"引擎模式绝不 WebAudio"。Linux 浏览器模式（无引擎）天然走 webaudio，不受影响。
- `backend="auto"` 值保留（兼容旧配置与设置页状态），语义映射为"engine 严格模式"；设置页展示为 ENGINE。
- bounce（WAV 导出）本就只走引擎，无需改动。
