# 前端动效设计必读规范（Physics & Fluid Animation Guide）

> 本文档是本工作区前端视觉交互与动效设计的核心基石与统一准则。所有新增组件、窗口、抽屉、菜单、卡片与微交互，均必须严格遵循本文档所定义的物理空间运动规律与 Q 弹流畅的动效哲学。

---

## 🎯 核心设计哲学

本软件的动画设计拒绝生硬机械的线性过渡，强调**“有重量、有弹性、有温度的真实物理空间手感”**。所有动效均需满足以下两大核心要求：

### 1. 强调真实物理空间的运动规律 (Physical Realism & Spatial Continuity)
- **质量与阻尼 (Mass & Damping)**：物体启动需要克服惯性，停下时需经过阻尼衰减，严禁匀速直线运动（Linear）。
- **动量与过冲 (Momentum & Overshoot)**：高速飞入或弹出的物体在到达终点时，会因动量产生轻微的过冲（Overshoot），随后被弹性势能拉回平衡位置。
- **空间因果与方向感知 (Directional Causality)**：
  - 展开与飞入的方向必须符合其触发源的空间位置（如：从卡片原位向外径向飞出、从底边中点向上起飞）；
  - 页面前进新页从右滑入（带惯性过冲）、后退新页从左滑入；
  - 撞击落点会对周围邻居产生向外的物理波纹推开与反向振荡（水波扩散效应）。
- **多维属性解耦 (Multi-track Decoupling)**：
  - **位移（Position/Translate）与尺寸（Scale/Size）独立解耦**：位移通常先落定（速度略快），尺寸形变略滞后于位移完全舒展，模拟柔软物体的气动/惯性形变。

### 2. 整体效果要 Q 弹流畅，符合逻辑 (Snappy, Bouncy & Logical Flow)
- **Q 弹可控**：过冲幅度严格克制在 3% ~ 8% 之间，既具备灵动 Q 弹的手感，又绝不拖泥带水，确保专业音频软件的高效敏捷。
- **时间与层级严密**：微交互（按钮点击、菜单弹出）在 180ms ~ 240ms 内完成；大型窗口/抽屉/面板在 380ms ~ 520ms 内完成；多子项出现采用 Stagger 阶梯延迟（每项间隔 30ms ~ 60ms）。
- **无状态撕裂**：入场与退场状态必须无缝闭环，关闭动画必须在视觉完全收拢后方可销毁或设为 `hidden`。

---

## 📐 核心贝塞尔曲线（Spring & Easing Matrix）

项目统一使用以下 4 大物理贝塞尔曲线，严禁使用浏览器原生未经调校的 `linear` 或简单 `ease`：

| 曲线类型 | 贝塞尔参数 `cubic-bezier` | 物理特性 | 适用场景 |
| :--- | :--- | :--- | :--- |
| **Bouncy Pop (Q 弹弹簧)** | `cubic-bezier(0.34, 1.45, 0.64, 1)` | 强弹性、明显回弹振荡（过冲约 45% 峰值势能），触感清脆 | 按钮点击反馈 (`.fn.pressed`)、HUD 悬浮徽章弹出、菜单展开 (`.menu-in`)、工具切换指示 |
| **Fluid Glide (流体过冲)** | `cubic-bezier(0.16, 1, 0.3, 1)` | 极快启动、平滑过冲 7%~10% 后柔和落定，大气沉稳 | 模态弹窗飞入 (`modal-fly`)、工作台控件起飞 (`dyn-in-fly`)、抽屉全屏最大化 |
| **Natural Spring (自然衰减)** | `cubic-bezier(0.22, 1, 0.36, 1)` | 柔和阻尼、零生硬感、单向平滑减速 | 底部卷帘抽屉展开/收起、卡片涟漪推开 (`card-push`)、列表 Stagger 进场 |
| **Snappy Collapse (敏捷收拢)** | `cubic-bezier(0.55, 0, 0.85, 0.36)` | 启动蓄力、极速收束退场、绝不滞留遮挡视线 | 弹窗坠落退场 (`modal-drop`)、控件收拢 (`dyn-out-collapse`)、菜单收起 |

---

## 🏛️ 全站动画设计方案与规范汇总

### 一、窗口与模态弹窗系统 (Modals & Dialogs)
- **入场方案**：
  - 遮罩层：`0.25s ease-out` 渐变淡入；
  - 弹窗本体：采用**双轨解耦**动画：
    - 位移轨迹：`0.40s cubic-bezier(0.16, 1, 0.3, 1)` 从下方 `translateY(90px)` 向上冲至 `-11px`（过冲 12%），再平稳落回 `0px`；
    - 尺寸形变：`0.50s cubic-bezier(0.16, 1, 0.3, 1)` 从 `scale(0.85)` 膨胀至 `scale(1.04)`，随后自然微缩恢复 `scale(1)`；
- **退场方案**：
  - 弹窗本体：`0.20s cubic-bezier(0.55, 0, 0.85, 0.36)` 快速向下轻坠 `translateY(28px) scale(0.94)` 并淡出；
  - 遮罩层：`0.20s ease-in` 同步淡出，完成后由 JS 置 `hidden = true`。

### 二、底部钢琴卷帘抽屉 (Piano Roll Drawer)
- **展开与收起**：
  - 抽屉整体使用 `transform: translateY(0)` 与 `translateY(100%)`；
  - 展开曲线使用 `transition: transform 0.36s cubic-bezier(0.22, 1.2, 0.36, 1)`，在接近完全拔起时带有 **5% 的轻盈 Q 弹向上过冲**，手感犹如卡簧就位；
  - 收起曲线使用 `0.24s cubic-bezier(0.55, 0, 0.85, 0.36)` 快速顺滑落入底部。
- **拖动手柄 (Resizer Handle)**：
  - 悬浮手柄时，手柄条宽度自适应弹性拉长 `48px -> 64px`，颜色自发光增强，给予清晰的“可拖拽”物理暗示。

### 三、实时悬浮徽章 (HUD Badge & Status Indicator)
- **出现与刷新**：
  - 每次参数变更（音符力度、移调、快捷键切换、工具切换）时，HUD 徽章触发 `@keyframes hud-pop`；
  - 耗时 `0.32s`，采用 `cubic-bezier(0.34, 1.45, 0.64, 1)` 从 `scale(0.82) translateY(6px)` 瞬间冲起至 `scale(1.06)`，再回弹落定；
  - 带来犹如掌机游戏般即时、爽快的交互反馈。

### 四、菜单与气泡弹出 (Context Menu & Select Dropdowns)
- **弹出规范**：
  - 添加 `.menu-in` 类，使用 `menu-pop 0.22s cubic-bezier(0.34, 1.45, 0.64, 1)`；
  - 从源点微缩偏置 `translateY(-6px) scale(0.96)` 瞬时展开并伴有柔和过冲；
  - 选项悬浮带有背景色平滑渐变与左侧缩进微动效（`padding-left: 12px -> 14px`）。

### 五、按钮与操作控件交互反馈 (Button Micro-interactions)
- **点击触感 (Press Feedback)**：
  - 点击工具按钮或模式切换按钮触发 `.pressed` / `active` 状态；
  - 产生 `@keyframes fn-press 0.35s cubic-bezier(0.34, 1.45, 0.64, 1)`：先向内瞬时微压 `scale(0.94)`，随后反弹至 `scale(1.05)`，最后落回 `scale(1.0)`；
- **状态高亮切换**：
  - 如 `🎹 键盘弹奏` 开启时，伴随橙色光晕脉冲与加粗显色，清晰传达模式切换。

### 六、卡片形态流转与水波涟漪 (FLIP & Ripple Dynamics)
- **卡片展开至工作台**：采用 JS FLIP 技术，从卡片实际矩形计算平移和缩放比例，工作台内部子元素沿径向方向依次飞出（`--spring-delay` 阶梯延迟）；
- **返回档案库**：卡片撞击原位时，触发四周相邻卡片的 `@keyframes card-push` 涟漪动画（推出 12px → 反向回弹 28% → 归位），模拟真实水波震荡。

---

## ⚡ 性能准则与无障碍规范

1. **只使用 GPU 合成层属性**：
   - 动画与过渡必须严格限制在 `transform`（`translate`, `scale`, `rotate`）与 `opacity` 两个属性上；
   - 严禁对 `width`, `height`, `top`, `left`, `margin`, `padding` 做每帧插值动画，防止触发浏览器重排（Layout Thrashing）。
2. **无障碍降级支持 (Prefers-Reduced-Motion)**：
   - 必须通过 `@media (prefers-reduced-motion: reduce)` 全局关闭或降级所有剧烈位移与缩放动画，直接使用瞬时切换或简单淡入淡出，保护对眩晕敏感的用户。
